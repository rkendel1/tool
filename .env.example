############
# GENERAL
############

# Studio defaults
STUDIO_DEFAULT_ORGANIZATION=Default Organization
STUDIO_DEFAULT_PROJECT=Default Project

# Public URLs - change these for production
SUPABASE_PUBLIC_URL=http://localhost:8000
API_EXTERNAL_URL=http://localhost:8000
SITE_URL=http://localhost:3000

############
# SUPABASE DATABASE
############

# Supabase Database configuration (PostgreSQL 15 with Supabase extensions)
POSTGRES_DB=postgres
POSTGRES_PASSWORD=your-super-secret-and-long-postgres-password

# Database schemas exposed via PostgREST API
PGRST_DB_SCHEMAS=public,storage,graphql_public

############
# JWT SECRETS
############

# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-token-with-at-least-32-characters-long
JWT_EXPIRY=3600

# Generate with: openssl rand -base64 32
REALTIME_SECRET_KEY_BASE=your-super-secret-realtime-key-base-with-at-least-32-characters-long

############
# API KEYS
############

# ANON_KEY - This key is safe to use in a browser if you have Row Level Security (RLS) enabled
# Generate using: https://supabase.com/docs/guides/hosting/overview#api-keys
# Or use the default for local development
ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyAgCiAgICAicm9sZSI6ICJhbm9uIiwKICAgICJpc3MiOiAic3VwYWJhc2UtZGVtbyIsCiAgICAiaWF0IjogMTY0MTc2OTIwMCwKICAgICJleHAiOiAxNzk5NTM1NjAwCn0.dc_X5iR_VP_qT0zsiyj_I_OZ2T9FtRU2BBNWN8Bu4GE

# SERVICE_ROLE_KEY - This key has full access to your data, keep it secure
# Generate using: https://supabase.com/docs/guides/hosting/overview#api-keys
# Or use the default for local development (NEVER use in production)
SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyAgCiAgICAicm9sZSI6ICJzZXJ2aWNlX3JvbGUiLAogICAgImlzcyI6ICJzdXBhYmFzZS1kZW1vIiwKICAgICJpYXQiOiAxNjQxNzY5MjAwLAogICAgImV4cCI6IDE3OTk1MzU2MDAKfQ.DaYlNEoUrrEn2Ig7tqibS-PHK5vgusbcbo7X36XVt4Q

############
# AUTH
############

# Email signup
ENABLE_EMAIL_SIGNUP=true
ENABLE_EMAIL_AUTOCONFIRM=true

# Phone signup
ENABLE_PHONE_SIGNUP=false
ENABLE_PHONE_AUTOCONFIRM=false

# Disable signup to restrict access
DISABLE_SIGNUP=false

# Redirect URLs - comma separated list
ADDITIONAL_REDIRECT_URLS=

# Email confirmation, password reset, etc.
MAILER_URLPATHS_INVITE=/auth/v1/verify
MAILER_URLPATHS_CONFIRMATION=/auth/v1/verify
MAILER_URLPATHS_RECOVERY=/auth/v1/verify
MAILER_URLPATHS_EMAIL_CHANGE=/auth/v1/verify

############
# SMTP (for sending emails)
############

# For local development, you can use a service like Mailhog or Mailtrap
# For production, use a real SMTP service like SendGrid, AWS SES, etc.

SMTP_ADMIN_EMAIL=admin@example.com
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=smtp_user
SMTP_PASS=smtp_password
SMTP_SENDER_NAME=Supabase

############
# STORAGE
############

# Image transformation
IMGPROXY_ENABLE_WEBP_DETECTION=true

############
# ANALYTICS
############

# Generate with: openssl rand -base64 32
LOGFLARE_API_KEY=your-super-secret-logflare-api-key

############
# PORTS (exposed to host)
############

# Kong API Gateway
KONG_HTTP_PORT=8000
KONG_HTTPS_PORT=8443

# Supabase Studio
STUDIO_PORT=3000

# Supabase Database
POSTGRES_PORT=54321

# Analytics (Logflare)
ANALYTICS_PORT=4000

# Redis
REDIS_PORT=6379

############
# PRODUCTION NOTES
############

# For production deployment:
# 1. Change all passwords and secrets to strong, unique values
# 2. Generate new JWT_SECRET and API keys
# 3. Update SUPABASE_PUBLIC_URL to your domain (e.g., https://api.yourdomain.com)
# 4. Update SITE_URL to your frontend URL (e.g., https://yourdomain.com)
# 5. Configure proper SMTP settings for email delivery
# 6. Enable SSL/TLS in Kong configuration
# 7. Set up proper backup strategies for the database
# 8. Review and configure Row Level Security (RLS) policies
# 9. Enable rate limiting and DDoS protection
# 10. Monitor logs and set up alerting

############
# VOLUME PATHS (optional overrides)
############

# Uncomment and modify if you want to change volume locations
# VOLUMES_PATH=./volumes
